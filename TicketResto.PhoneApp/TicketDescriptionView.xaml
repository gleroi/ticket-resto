<UserControl
    x:Class="TicketResto.PhoneApp.TicketDescriptionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TicketResto.PhoneApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="75"
    d:DesignWidth="400">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <TextBox x:Name="Value" InputScope="Number"  />
        
        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <AppBarButton x:Name="DecreaseQuantity" Icon="Remove" />
            <TextBlock x:Name="MaxQuantity" FontSize="30" Width="35"
                       VerticalAlignment="Center"
                       TextAlignment="Center" />
            <AppBarButton x:Name="IncreaseQuantity" Icon="Add" />
        </StackPanel>
        
    </Grid>
</UserControl>
